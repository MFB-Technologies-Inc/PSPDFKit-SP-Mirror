name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - "*"
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    environment: default
    steps:
      - uses: actions/checkout@v6
      - name: Resolve frameworks
        run: swift package resolve
